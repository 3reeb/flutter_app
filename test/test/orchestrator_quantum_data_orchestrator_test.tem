import 'dart:convert';

import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:quantum_layout/quantum.dart';

import 'support/test_helpers.dart';

void main() {
  setUp(resetQuantumRuntime);

  // testWidgets(
  //     'QuantumDataOrchestrator bootstraps state, schemas, pipelines and actions',
  //     (tester) async {
  //   await tester.pumpWidget(
  //     const Directionality(
  //       textDirection: TextDirection.ltr,
  //       child: SizedBox(width: 10, height: 10),
  //     ),
  //   );
  //   final context = tester.element(find.byType(SizedBox));

  //   await QuantumDataOrchestrator.bootstrap(sampleManifest(), context);
  //   await Future<void>.delayed(Duration.zero);

  //   final store = QLStoreRegistry.instance.get('shop');
  //   expect(store.get('counter'), 1);
  //   expect(QLSchemaRegistry.instance.hasSchema('product'), isTrue);
  //   expect(QLPipelineRegistry.instance.exists('products'), isTrue);
  //   expect(QuantumVM.instance.hasAction('shop.mark'), isTrue);
  // });

  // testWidgets('QuantumDataOrchestrator bootstrapString parses JSON manifests',
  //     (tester) async {
  //   await tester.pumpWidget(
  //     const Directionality(
  //       textDirection: TextDirection.ltr,
  //       child: SizedBox(width: 1, height: 1),
  //     ),
  //   );
  //   final context = tester.element(find.byType(SizedBox));

  //   final manifest = jsonEncode({
  //     'module': 'jsonModule',
  //     'state': {'flag': true},
  //     'schemas': {
  //       'item': {
  //         'id': {'type': 'string', 'required': true},
  //       }
  //     }
  //   });

  //   await QuantumDataOrchestrator.bootstrapString(manifest, context);
  //   expect(QLStoreRegistry.instance.get('jsonModule').get('flag'), isTrue);
  //   expect(QLSchemaRegistry.instance.hasSchema('item'), isTrue);
  // });

  // testWidgets(
  //     'QuantumDataOrchestrator handles nested modules using the same resolved context',
  //     (tester) async {
  //   await tester.pumpWidget(
  //     const Directionality(
  //       textDirection: TextDirection.ltr,
  //       child: SizedBox(width: 1, height: 1),
  //     ),
  //   );
  //   final context = tester.element(find.byType(SizedBox));

  //   await QuantumDataOrchestrator.bootstrap({
  //     'module': 'parent',
  //     'modules': [
  //       {
  //         'module': 'child',
  //         'state': {'value': 7}
  //       },
  //     ],
  //   }, context);

  //   expect(QLStoreRegistry.instance.get('child').get('value'), 7);
  // });
}
