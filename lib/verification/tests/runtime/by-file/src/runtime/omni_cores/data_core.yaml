package:
  name: runtime_generated_tests
  file: lib/src/runtime/omni_cores/data_core.dart
  docs_file: docs/src/runtime/omni_cores/data_core.dart.md
  source_entry:
    line_count: 311
    byte_count: 12922
    class_count: 0
    enum_count: 0
    function_count: 3
    category: state
    test_count: 8
  scenario_count: 8
source:
  path: src/runtime/omni_cores/data_core.dart
  docs_path: docs/src/runtime/omni_cores/data_core.dart.md
  label: src/runtime/omni_cores/data_core.dart
scenarios:
- uuid: df3302cc-d162-558d-8439-4d9e825c1d70
  file: lib/src/runtime/omni_cores/data_core.dart
  docs_file: docs/src/runtime/omni_cores/data_core.dart.md
  title: 'Data Core: public contract remains stable under valid input'
  slug: contract
  category: state
  severity: critical
  description: Verify that data core keeps its documented runtime behavior while handling contract conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/runtime/omni_cores/data_core.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/runtime/omni_cores/data_core.dart.md.
    stimulus:
    - Drive the registry/cache/state layer through hit, miss, mutation, and teardown cycles.
    - Repeat updates concurrently to expose stale snapshot or listener leaks.
    data:
      workload:
      - single write
      - parallel writes
      - rapid reads
      - eviction pressure
  expected_output:
    state: Snapshots remain internally consistent and deterministic.
    cleanup: Detached listeners and expired entries are reclaimed.
    performance: Hot-path operations stay bounded under repeated calls.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 100
    stress_ms: 250
  memory_budget:
    max_growth_mb: 1
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: A core primitives file. These modules define low-level types, flags, value objects, or utility abstractions used throughout the framework.
    headings:
    - '`src/runtime/omni_cores/data_core.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - state
  - contract
  - data core
- uuid: 10b2e60a-d064-5ece-80fc-1dc3e00f14ca
  file: lib/src/runtime/omni_cores/data_core.dart
  docs_file: docs/src/runtime/omni_cores/data_core.dart.md
  title: 'Data Core: invalid or malformed input is rejected cleanly'
  slug: invalid_input
  category: state
  severity: critical
  description: Verify that data core keeps its documented runtime behavior while handling invalid input conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/runtime/omni_cores/data_core.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/runtime/omni_cores/data_core.dart.md.
    stimulus:
    - Drive the registry/cache/state layer through hit, miss, mutation, and teardown cycles.
    - Repeat updates concurrently to expose stale snapshot or listener leaks.
    data:
      workload:
      - single write
      - parallel writes
      - rapid reads
      - eviction pressure
  expected_output:
    state: Snapshots remain internally consistent and deterministic.
    cleanup: Detached listeners and expired entries are reclaimed.
    performance: Hot-path operations stay bounded under repeated calls.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 100
    stress_ms: 250
  memory_budget:
    max_growth_mb: 1
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: A core primitives file. These modules define low-level types, flags, value objects, or utility abstractions used throughout the framework.
    headings:
    - '`src/runtime/omni_cores/data_core.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - state
  - invalid_input
  - data core
- uuid: 6f099bbb-63ea-577d-8864-9204b545886d
  file: lib/src/runtime/omni_cores/data_core.dart
  docs_file: docs/src/runtime/omni_cores/data_core.dart.md
  title: 'Data Core: re-entrant calls do not corrupt internal state'
  slug: reentrant
  category: state
  severity: high
  description: Verify that data core keeps its documented runtime behavior while handling reentrant conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/runtime/omni_cores/data_core.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/runtime/omni_cores/data_core.dart.md.
    stimulus:
    - Drive the registry/cache/state layer through hit, miss, mutation, and teardown cycles.
    - Repeat updates concurrently to expose stale snapshot or listener leaks.
    data:
      workload:
      - single write
      - parallel writes
      - rapid reads
      - eviction pressure
  expected_output:
    state: Snapshots remain internally consistent and deterministic.
    cleanup: Detached listeners and expired entries are reclaimed.
    performance: Hot-path operations stay bounded under repeated calls.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 100
    stress_ms: 250
  memory_budget:
    max_growth_mb: 1
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: A core primitives file. These modules define low-level types, flags, value objects, or utility abstractions used throughout the framework.
    headings:
    - '`src/runtime/omni_cores/data_core.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - state
  - reentrant
  - data core
- uuid: 1abc632c-d152-5eee-b7ff-86ded461ff31
  file: lib/src/runtime/omni_cores/data_core.dart
  docs_file: docs/src/runtime/omni_cores/data_core.dart.md
  title: 'Data Core: dispose/close/teardown releases resources deterministically'
  slug: cleanup
  category: state
  severity: high
  description: Verify that data core keeps its documented runtime behavior while handling cleanup conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/runtime/omni_cores/data_core.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/runtime/omni_cores/data_core.dart.md.
    stimulus:
    - Drive the registry/cache/state layer through hit, miss, mutation, and teardown cycles.
    - Repeat updates concurrently to expose stale snapshot or listener leaks.
    data:
      workload:
      - single write
      - parallel writes
      - rapid reads
      - eviction pressure
  expected_output:
    state: Snapshots remain internally consistent and deterministic.
    cleanup: Detached listeners and expired entries are reclaimed.
    performance: Hot-path operations stay bounded under repeated calls.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 100
    stress_ms: 250
  memory_budget:
    max_growth_mb: 1
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: A core primitives file. These modules define low-level types, flags, value objects, or utility abstractions used throughout the framework.
    headings:
    - '`src/runtime/omni_cores/data_core.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - state
  - cleanup
  - data core
- uuid: 43c733be-3ea7-5dea-bae1-63c6bbf54e0b
  file: lib/src/runtime/omni_cores/data_core.dart
  docs_file: docs/src/runtime/omni_cores/data_core.dart.md
  title: 'Data Core: hot-path behavior stays within the runtime budget'
  slug: performance
  category: state
  severity: high
  description: Verify that data core keeps its documented runtime behavior while handling performance conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/runtime/omni_cores/data_core.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/runtime/omni_cores/data_core.dart.md.
    stimulus:
    - Drive the registry/cache/state layer through hit, miss, mutation, and teardown cycles.
    - Repeat updates concurrently to expose stale snapshot or listener leaks.
    data:
      workload:
      - single write
      - parallel writes
      - rapid reads
      - eviction pressure
  expected_output:
    state: Snapshots remain internally consistent and deterministic.
    cleanup: Detached listeners and expired entries are reclaimed.
    performance: Hot-path operations stay bounded under repeated calls.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 100
    stress_ms: 250
  memory_budget:
    max_growth_mb: 1
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: A core primitives file. These modules define low-level types, flags, value objects, or utility abstractions used throughout the framework.
    headings:
    - '`src/runtime/omni_cores/data_core.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - state
  - performance
  - data core
- uuid: 4e875b49-7e32-53a8-8ff6-dab033782d7b
  file: lib/src/runtime/omni_cores/data_core.dart
  docs_file: docs/src/runtime/omni_cores/data_core.dart.md
  title: 'Data Core: memory usage stays bounded under repeated operations'
  slug: memory
  category: state
  severity: medium
  description: Verify that data core keeps its documented runtime behavior while handling memory conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/runtime/omni_cores/data_core.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/runtime/omni_cores/data_core.dart.md.
    stimulus:
    - Drive the registry/cache/state layer through hit, miss, mutation, and teardown cycles.
    - Repeat updates concurrently to expose stale snapshot or listener leaks.
    data:
      workload:
      - single write
      - parallel writes
      - rapid reads
      - eviction pressure
  expected_output:
    state: Snapshots remain internally consistent and deterministic.
    cleanup: Detached listeners and expired entries are reclaimed.
    performance: Hot-path operations stay bounded under repeated calls.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 100
    stress_ms: 250
  memory_budget:
    max_growth_mb: 1
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: A core primitives file. These modules define low-level types, flags, value objects, or utility abstractions used throughout the framework.
    headings:
    - '`src/runtime/omni_cores/data_core.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - state
  - memory
  - data core
- uuid: 9d2d4c20-58a4-58f1-987f-470580354719
  file: lib/src/runtime/omni_cores/data_core.dart
  docs_file: docs/src/runtime/omni_cores/data_core.dart.md
  title: 'Data Core: the runtime surface still matches the documented API and headings'
  slug: docs_parity
  category: state
  severity: medium
  description: Verify that data core keeps its documented runtime behavior while handling docs parity conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/runtime/omni_cores/data_core.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/runtime/omni_cores/data_core.dart.md.
    stimulus:
    - Drive the registry/cache/state layer through hit, miss, mutation, and teardown cycles.
    - Repeat updates concurrently to expose stale snapshot or listener leaks.
    data:
      workload:
      - single write
      - parallel writes
      - rapid reads
      - eviction pressure
  expected_output:
    state: Snapshots remain internally consistent and deterministic.
    cleanup: Detached listeners and expired entries are reclaimed.
    performance: Hot-path operations stay bounded under repeated calls.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 100
    stress_ms: 250
  memory_budget:
    max_growth_mb: 1
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: A core primitives file. These modules define low-level types, flags, value objects, or utility abstractions used throughout the framework.
    headings:
    - '`src/runtime/omni_cores/data_core.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - state
  - docs_parity
  - data core
- uuid: 02002cc3-7b14-5144-8c42-6a895bf807e7
  file: lib/src/runtime/omni_cores/data_core.dart
  docs_file: docs/src/runtime/omni_cores/data_core.dart.md
  title: 'Data Core: cache hit, miss, eviction, and invalidation behavior is deterministic'
  slug: cache
  category: state
  severity: medium
  description: Verify that data core keeps its documented runtime behavior while handling cache conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/runtime/omni_cores/data_core.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/runtime/omni_cores/data_core.dart.md.
    stimulus:
    - Drive the registry/cache/state layer through hit, miss, mutation, and teardown cycles.
    - Repeat updates concurrently to expose stale snapshot or listener leaks.
    data:
      workload:
      - single write
      - parallel writes
      - rapid reads
      - eviction pressure
  expected_output:
    state: Snapshots remain internally consistent and deterministic.
    cleanup: Detached listeners and expired entries are reclaimed.
    performance: Hot-path operations stay bounded under repeated calls.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 100
    stress_ms: 250
  memory_budget:
    max_growth_mb: 1
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: A core primitives file. These modules define low-level types, flags, value objects, or utility abstractions used throughout the framework.
    headings:
    - '`src/runtime/omni_cores/data_core.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - state
  - cache
  - data core
