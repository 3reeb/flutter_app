package:
  name: runtime_generated_tests
  file: lib/src/foundation/quantum_reactive_graph.dart
  docs_file: docs/src/foundation/quantum_reactive_graph.dart.md
  source_entry:
    line_count: 651
    byte_count: 23568
    class_count: 7
    enum_count: 0
    function_count: 20
    category: ui
    test_count: 10
  scenario_count: 10
source:
  path: src/foundation/quantum_reactive_graph.dart
  docs_path: docs/src/foundation/quantum_reactive_graph.dart.md
  label: src/foundation/quantum_reactive_graph.dart
scenarios:
- uuid: 8ca6b844-e596-5886-b219-a2e73060219a
  file: lib/src/foundation/quantum_reactive_graph.dart
  docs_file: docs/src/foundation/quantum_reactive_graph.dart.md
  title: 'Quantum Reactive Graph: public contract remains stable under valid input'
  slug: contract
  category: ui
  severity: critical
  description: Verify that quantum reactive graph keeps its documented runtime behavior while handling contract conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_reactive_graph.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_reactive_graph.dart.md.
    stimulus:
    - Mount the widget or render object under multiple constraint sets.
    - Trigger rebuilds, pointer interaction, and teardown in a single test flow.
    data:
      constraints:
      - tight
      - loose
      - unbounded
      - normal
      gestures:
      - tap
      - scroll
  expected_output:
    layout: No overflow, clipping regression, or unexpected relayout churn.
    interaction: Hit-testing and gesture routing remain deterministic.
    frame_budget: Frame timing stays within the defined budget on repeated rebuilds.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A foundation module. These files define the base reactive state, async primitives, parsing utilities, data structures, math helpers, and error-handling machinery used everywhere else. Author-intent note: QUANTUM REACTIVE GRAPH ENGINE v1.0 — SIGNAL-DRIVEN ANIMATION COMPOSITOR'
    headings:
    - '`src/foundation/quantum_reactive_graph.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - ui
  - contract
  - quantum reactive graph
- uuid: 80f458d3-8223-5dc4-8b90-2d860d109497
  file: lib/src/foundation/quantum_reactive_graph.dart
  docs_file: docs/src/foundation/quantum_reactive_graph.dart.md
  title: 'Quantum Reactive Graph: invalid or malformed input is rejected cleanly'
  slug: invalid_input
  category: ui
  severity: critical
  description: Verify that quantum reactive graph keeps its documented runtime behavior while handling invalid input conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_reactive_graph.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_reactive_graph.dart.md.
    stimulus:
    - Mount the widget or render object under multiple constraint sets.
    - Trigger rebuilds, pointer interaction, and teardown in a single test flow.
    data:
      constraints:
      - tight
      - loose
      - unbounded
      - rotated
      gestures:
      - tap
      - scroll
  expected_output:
    layout: No overflow, clipping regression, or unexpected relayout churn.
    interaction: Hit-testing and gesture routing remain deterministic.
    frame_budget: Frame timing stays within the defined budget on repeated rebuilds.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A foundation module. These files define the base reactive state, async primitives, parsing utilities, data structures, math helpers, and error-handling machinery used everywhere else. Author-intent note: QUANTUM REACTIVE GRAPH ENGINE v1.0 — SIGNAL-DRIVEN ANIMATION COMPOSITOR'
    headings:
    - '`src/foundation/quantum_reactive_graph.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - ui
  - invalid_input
  - quantum reactive graph
- uuid: fa1a2db8-f1de-5016-8db1-bc3effe6e37b
  file: lib/src/foundation/quantum_reactive_graph.dart
  docs_file: docs/src/foundation/quantum_reactive_graph.dart.md
  title: 'Quantum Reactive Graph: re-entrant calls do not corrupt internal state'
  slug: reentrant
  category: ui
  severity: high
  description: Verify that quantum reactive graph keeps its documented runtime behavior while handling reentrant conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_reactive_graph.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_reactive_graph.dart.md.
    stimulus:
    - Mount the widget or render object under multiple constraint sets.
    - Trigger rebuilds, pointer interaction, and teardown in a single test flow.
    data:
      constraints:
      - tight
      - loose
      - unbounded
      - normal
      gestures:
      - tap
      - scroll
  expected_output:
    layout: No overflow, clipping regression, or unexpected relayout churn.
    interaction: Hit-testing and gesture routing remain deterministic.
    frame_budget: Frame timing stays within the defined budget on repeated rebuilds.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A foundation module. These files define the base reactive state, async primitives, parsing utilities, data structures, math helpers, and error-handling machinery used everywhere else. Author-intent note: QUANTUM REACTIVE GRAPH ENGINE v1.0 — SIGNAL-DRIVEN ANIMATION COMPOSITOR'
    headings:
    - '`src/foundation/quantum_reactive_graph.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - ui
  - reentrant
  - quantum reactive graph
- uuid: 4f635391-4ac7-5409-a000-e3f2ba55529a
  file: lib/src/foundation/quantum_reactive_graph.dart
  docs_file: docs/src/foundation/quantum_reactive_graph.dart.md
  title: 'Quantum Reactive Graph: dispose/close/teardown releases resources deterministically'
  slug: cleanup
  category: ui
  severity: high
  description: Verify that quantum reactive graph keeps its documented runtime behavior while handling cleanup conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_reactive_graph.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_reactive_graph.dart.md.
    stimulus:
    - Mount the widget or render object under multiple constraint sets.
    - Trigger rebuilds, pointer interaction, and teardown in a single test flow.
    data:
      constraints:
      - tight
      - loose
      - unbounded
      - rotated
      gestures:
      - tap
      - scroll
  expected_output:
    layout: No overflow, clipping regression, or unexpected relayout churn.
    interaction: Hit-testing and gesture routing remain deterministic.
    frame_budget: Frame timing stays within the defined budget on repeated rebuilds.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A foundation module. These files define the base reactive state, async primitives, parsing utilities, data structures, math helpers, and error-handling machinery used everywhere else. Author-intent note: QUANTUM REACTIVE GRAPH ENGINE v1.0 — SIGNAL-DRIVEN ANIMATION COMPOSITOR'
    headings:
    - '`src/foundation/quantum_reactive_graph.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - ui
  - cleanup
  - quantum reactive graph
- uuid: b7b33e43-ab99-5695-b950-e6739ef51c21
  file: lib/src/foundation/quantum_reactive_graph.dart
  docs_file: docs/src/foundation/quantum_reactive_graph.dart.md
  title: 'Quantum Reactive Graph: hot-path behavior stays within the runtime budget'
  slug: performance
  category: ui
  severity: high
  description: Verify that quantum reactive graph keeps its documented runtime behavior while handling performance conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_reactive_graph.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_reactive_graph.dart.md.
    stimulus:
    - Mount the widget or render object under multiple constraint sets.
    - Trigger rebuilds, pointer interaction, and teardown in a single test flow.
    data:
      constraints:
      - tight
      - loose
      - unbounded
      - normal
      gestures:
      - tap
      - scroll
  expected_output:
    layout: No overflow, clipping regression, or unexpected relayout churn.
    interaction: Hit-testing and gesture routing remain deterministic.
    frame_budget: Frame timing stays within the defined budget on repeated rebuilds.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A foundation module. These files define the base reactive state, async primitives, parsing utilities, data structures, math helpers, and error-handling machinery used everywhere else. Author-intent note: QUANTUM REACTIVE GRAPH ENGINE v1.0 — SIGNAL-DRIVEN ANIMATION COMPOSITOR'
    headings:
    - '`src/foundation/quantum_reactive_graph.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - ui
  - performance
  - quantum reactive graph
- uuid: 2bf26b2f-7642-5679-a111-923555376df9
  file: lib/src/foundation/quantum_reactive_graph.dart
  docs_file: docs/src/foundation/quantum_reactive_graph.dart.md
  title: 'Quantum Reactive Graph: memory usage stays bounded under repeated operations'
  slug: memory
  category: ui
  severity: medium
  description: Verify that quantum reactive graph keeps its documented runtime behavior while handling memory conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_reactive_graph.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_reactive_graph.dart.md.
    stimulus:
    - Mount the widget or render object under multiple constraint sets.
    - Trigger rebuilds, pointer interaction, and teardown in a single test flow.
    data:
      constraints:
      - tight
      - loose
      - unbounded
      - rotated
      gestures:
      - tap
      - scroll
  expected_output:
    layout: No overflow, clipping regression, or unexpected relayout churn.
    interaction: Hit-testing and gesture routing remain deterministic.
    frame_budget: Frame timing stays within the defined budget on repeated rebuilds.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A foundation module. These files define the base reactive state, async primitives, parsing utilities, data structures, math helpers, and error-handling machinery used everywhere else. Author-intent note: QUANTUM REACTIVE GRAPH ENGINE v1.0 — SIGNAL-DRIVEN ANIMATION COMPOSITOR'
    headings:
    - '`src/foundation/quantum_reactive_graph.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - ui
  - memory
  - quantum reactive graph
- uuid: f8135b6a-264d-50ad-8379-0ff0b39f8ba7
  file: lib/src/foundation/quantum_reactive_graph.dart
  docs_file: docs/src/foundation/quantum_reactive_graph.dart.md
  title: 'Quantum Reactive Graph: the runtime surface still matches the documented API and headings'
  slug: docs_parity
  category: ui
  severity: medium
  description: Verify that quantum reactive graph keeps its documented runtime behavior while handling docs parity conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_reactive_graph.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_reactive_graph.dart.md.
    stimulus:
    - Mount the widget or render object under multiple constraint sets.
    - Trigger rebuilds, pointer interaction, and teardown in a single test flow.
    data:
      constraints:
      - tight
      - loose
      - unbounded
      - normal
      gestures:
      - tap
      - scroll
  expected_output:
    layout: No overflow, clipping regression, or unexpected relayout churn.
    interaction: Hit-testing and gesture routing remain deterministic.
    frame_budget: Frame timing stays within the defined budget on repeated rebuilds.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A foundation module. These files define the base reactive state, async primitives, parsing utilities, data structures, math helpers, and error-handling machinery used everywhere else. Author-intent note: QUANTUM REACTIVE GRAPH ENGINE v1.0 — SIGNAL-DRIVEN ANIMATION COMPOSITOR'
    headings:
    - '`src/foundation/quantum_reactive_graph.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - ui
  - docs_parity
  - quantum reactive graph
- uuid: a22227b6-5b05-5243-b0cf-9f2c9543e463
  file: lib/src/foundation/quantum_reactive_graph.dart
  docs_file: docs/src/foundation/quantum_reactive_graph.dart.md
  title: 'Quantum Reactive Graph: layout remains correct under narrow, wide, and constrained bounds'
  slug: layout
  category: ui
  severity: medium
  description: Verify that quantum reactive graph keeps its documented runtime behavior while handling layout conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_reactive_graph.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_reactive_graph.dart.md.
    stimulus:
    - Mount the widget or render object under multiple constraint sets.
    - Trigger rebuilds, pointer interaction, and teardown in a single test flow.
    data:
      constraints:
      - tight
      - loose
      - unbounded
      - rotated
      gestures:
      - tap
      - scroll
  expected_output:
    layout: No overflow, clipping regression, or unexpected relayout churn.
    interaction: Hit-testing and gesture routing remain deterministic.
    frame_budget: Frame timing stays within the defined budget on repeated rebuilds.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A foundation module. These files define the base reactive state, async primitives, parsing utilities, data structures, math helpers, and error-handling machinery used everywhere else. Author-intent note: QUANTUM REACTIVE GRAPH ENGINE v1.0 — SIGNAL-DRIVEN ANIMATION COMPOSITOR'
    headings:
    - '`src/foundation/quantum_reactive_graph.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - ui
  - layout
  - quantum reactive graph
- uuid: 385379a9-7502-5aa7-ab08-b9182cf01ec3
  file: lib/src/foundation/quantum_reactive_graph.dart
  docs_file: docs/src/foundation/quantum_reactive_graph.dart.md
  title: 'Quantum Reactive Graph: semantics and accessibility output remain stable across rebuilds'
  slug: semantics
  category: ui
  severity: medium
  description: Verify that quantum reactive graph keeps its documented runtime behavior while handling semantics conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_reactive_graph.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_reactive_graph.dart.md.
    stimulus:
    - Mount the widget or render object under multiple constraint sets.
    - Trigger rebuilds, pointer interaction, and teardown in a single test flow.
    data:
      constraints:
      - tight
      - loose
      - unbounded
      - normal
      gestures:
      - tap
      - scroll
  expected_output:
    layout: No overflow, clipping regression, or unexpected relayout churn.
    interaction: Hit-testing and gesture routing remain deterministic.
    frame_budget: Frame timing stays within the defined budget on repeated rebuilds.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A foundation module. These files define the base reactive state, async primitives, parsing utilities, data structures, math helpers, and error-handling machinery used everywhere else. Author-intent note: QUANTUM REACTIVE GRAPH ENGINE v1.0 — SIGNAL-DRIVEN ANIMATION COMPOSITOR'
    headings:
    - '`src/foundation/quantum_reactive_graph.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - ui
  - semantics
  - quantum reactive graph
- uuid: b08dd610-fb82-5955-8a3a-05acd988a165
  file: lib/src/foundation/quantum_reactive_graph.dart
  docs_file: docs/src/foundation/quantum_reactive_graph.dart.md
  title: 'Quantum Reactive Graph: gesture handling and hit testing stay correct during interaction'
  slug: gesture
  category: ui
  severity: medium
  description: Verify that quantum reactive graph keeps its documented runtime behavior while handling gesture conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_reactive_graph.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_reactive_graph.dart.md.
    stimulus:
    - Mount the widget or render object under multiple constraint sets.
    - Trigger rebuilds, pointer interaction, and teardown in a single test flow.
    data:
      constraints:
      - tight
      - loose
      - unbounded
      - rotated
      gestures:
      - tap
      - hover
      - drag
      - resize
  expected_output:
    layout: No overflow, clipping regression, or unexpected relayout churn.
    interaction: Hit-testing and gesture routing remain deterministic.
    frame_budget: Frame timing stays within the defined budget on repeated rebuilds.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A foundation module. These files define the base reactive state, async primitives, parsing utilities, data structures, math helpers, and error-handling machinery used everywhere else. Author-intent note: QUANTUM REACTIVE GRAPH ENGINE v1.0 — SIGNAL-DRIVEN ANIMATION COMPOSITOR'
    headings:
    - '`src/foundation/quantum_reactive_graph.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - ui
  - gesture
  - quantum reactive graph
