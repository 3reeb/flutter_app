package:
  name: runtime_generated_tests
  file: lib/src/foundation/quantum_core.dart
  docs_file: docs/src/foundation/quantum_core.dart.md
  source_entry:
    line_count: 657
    byte_count: 19454
    class_count: 13
    enum_count: 1
    function_count: 3
    category: runtime
    test_count: 10
  scenario_count: 10
source:
  path: src/foundation/quantum_core.dart
  docs_path: docs/src/foundation/quantum_core.dart.md
  label: src/foundation/quantum_core.dart
scenarios:
- uuid: 1cf3d636-3ee5-5298-82f0-20fa191dae3d
  file: lib/src/foundation/quantum_core.dart
  docs_file: docs/src/foundation/quantum_core.dart.md
  title: 'Quantum Core: public contract remains stable under valid input'
  slug: contract
  category: runtime
  severity: critical
  description: Verify that quantum core keeps its documented runtime behavior while handling contract conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_core.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_core.dart.md.
    stimulus:
    - Drive the registry/cache/state layer through hit, miss, mutation, and teardown cycles.
    - Repeat updates concurrently to expose stale snapshot or listener leaks.
    data:
      workload:
      - single write
      - parallel writes
      - rapid reads
      - eviction pressure
  expected_output:
    state: Snapshots remain internally consistent and deterministic.
    cleanup: Detached listeners and expired entries are reclaimed.
    performance: Hot-path operations stay bounded under repeated calls.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 100
    stress_ms: 250
  memory_budget:
    max_growth_mb: 1
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'This file defines the shared low-level primitives that the rest of the framework depends on: state flags, sleep policy, node errors, field type constants, field flags, projection helpers, path utilities, and a few compact math / parsing helpers. It is the first place to look when a higher layer needs to know what a field type or runtime bit flag means.'
    headings:
    - '`src/foundation/quantum_core.dart`'
    - What this file is
    - Core responsibilities
    - Key field types
    - Field flags
  tags:
  - runtime
  - contract
  - quantum core
- uuid: c98b7631-2c05-5dd8-98fe-3c7dfbb18abc
  file: lib/src/foundation/quantum_core.dart
  docs_file: docs/src/foundation/quantum_core.dart.md
  title: 'Quantum Core: invalid or malformed input is rejected cleanly'
  slug: invalid_input
  category: runtime
  severity: critical
  description: Verify that quantum core keeps its documented runtime behavior while handling invalid input conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_core.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_core.dart.md.
    stimulus:
    - Drive the registry/cache/state layer through hit, miss, mutation, and teardown cycles.
    - Repeat updates concurrently to expose stale snapshot or listener leaks.
    data:
      workload:
      - single write
      - parallel writes
      - rapid reads
      - eviction pressure
  expected_output:
    state: Snapshots remain internally consistent and deterministic.
    cleanup: Detached listeners and expired entries are reclaimed.
    performance: Hot-path operations stay bounded under repeated calls.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 100
    stress_ms: 250
  memory_budget:
    max_growth_mb: 1
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'This file defines the shared low-level primitives that the rest of the framework depends on: state flags, sleep policy, node errors, field type constants, field flags, projection helpers, path utilities, and a few compact math / parsing helpers. It is the first place to look when a higher layer needs to know what a field type or runtime bit flag means.'
    headings:
    - '`src/foundation/quantum_core.dart`'
    - What this file is
    - Core responsibilities
    - Key field types
    - Field flags
  tags:
  - runtime
  - invalid_input
  - quantum core
- uuid: c812f740-28c1-5b8c-8298-600e1c868786
  file: lib/src/foundation/quantum_core.dart
  docs_file: docs/src/foundation/quantum_core.dart.md
  title: 'Quantum Core: re-entrant calls do not corrupt internal state'
  slug: reentrant
  category: runtime
  severity: high
  description: Verify that quantum core keeps its documented runtime behavior while handling reentrant conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_core.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_core.dart.md.
    stimulus:
    - Drive the registry/cache/state layer through hit, miss, mutation, and teardown cycles.
    - Repeat updates concurrently to expose stale snapshot or listener leaks.
    data:
      workload:
      - single write
      - parallel writes
      - rapid reads
      - eviction pressure
  expected_output:
    state: Snapshots remain internally consistent and deterministic.
    cleanup: Detached listeners and expired entries are reclaimed.
    performance: Hot-path operations stay bounded under repeated calls.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 100
    stress_ms: 250
  memory_budget:
    max_growth_mb: 1
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'This file defines the shared low-level primitives that the rest of the framework depends on: state flags, sleep policy, node errors, field type constants, field flags, projection helpers, path utilities, and a few compact math / parsing helpers. It is the first place to look when a higher layer needs to know what a field type or runtime bit flag means.'
    headings:
    - '`src/foundation/quantum_core.dart`'
    - What this file is
    - Core responsibilities
    - Key field types
    - Field flags
  tags:
  - runtime
  - reentrant
  - quantum core
- uuid: 3924debb-9de5-5cd1-b1a2-5ab12547b29d
  file: lib/src/foundation/quantum_core.dart
  docs_file: docs/src/foundation/quantum_core.dart.md
  title: 'Quantum Core: dispose/close/teardown releases resources deterministically'
  slug: cleanup
  category: runtime
  severity: high
  description: Verify that quantum core keeps its documented runtime behavior while handling cleanup conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_core.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_core.dart.md.
    stimulus:
    - Drive the registry/cache/state layer through hit, miss, mutation, and teardown cycles.
    - Repeat updates concurrently to expose stale snapshot or listener leaks.
    data:
      workload:
      - single write
      - parallel writes
      - rapid reads
      - eviction pressure
  expected_output:
    state: Snapshots remain internally consistent and deterministic.
    cleanup: Detached listeners and expired entries are reclaimed.
    performance: Hot-path operations stay bounded under repeated calls.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 100
    stress_ms: 250
  memory_budget:
    max_growth_mb: 1
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'This file defines the shared low-level primitives that the rest of the framework depends on: state flags, sleep policy, node errors, field type constants, field flags, projection helpers, path utilities, and a few compact math / parsing helpers. It is the first place to look when a higher layer needs to know what a field type or runtime bit flag means.'
    headings:
    - '`src/foundation/quantum_core.dart`'
    - What this file is
    - Core responsibilities
    - Key field types
    - Field flags
  tags:
  - runtime
  - cleanup
  - quantum core
- uuid: 270aa6c5-1396-5118-8f6e-b09f1c91e20e
  file: lib/src/foundation/quantum_core.dart
  docs_file: docs/src/foundation/quantum_core.dart.md
  title: 'Quantum Core: hot-path behavior stays within the runtime budget'
  slug: performance
  category: runtime
  severity: high
  description: Verify that quantum core keeps its documented runtime behavior while handling performance conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_core.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_core.dart.md.
    stimulus:
    - Drive the registry/cache/state layer through hit, miss, mutation, and teardown cycles.
    - Repeat updates concurrently to expose stale snapshot or listener leaks.
    data:
      workload:
      - single write
      - parallel writes
      - rapid reads
      - eviction pressure
  expected_output:
    state: Snapshots remain internally consistent and deterministic.
    cleanup: Detached listeners and expired entries are reclaimed.
    performance: Hot-path operations stay bounded under repeated calls.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 100
    stress_ms: 250
  memory_budget:
    max_growth_mb: 1
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'This file defines the shared low-level primitives that the rest of the framework depends on: state flags, sleep policy, node errors, field type constants, field flags, projection helpers, path utilities, and a few compact math / parsing helpers. It is the first place to look when a higher layer needs to know what a field type or runtime bit flag means.'
    headings:
    - '`src/foundation/quantum_core.dart`'
    - What this file is
    - Core responsibilities
    - Key field types
    - Field flags
  tags:
  - runtime
  - performance
  - quantum core
- uuid: 12d0f5fd-894c-5af3-912f-856fc3fb7a77
  file: lib/src/foundation/quantum_core.dart
  docs_file: docs/src/foundation/quantum_core.dart.md
  title: 'Quantum Core: memory usage stays bounded under repeated operations'
  slug: memory
  category: runtime
  severity: medium
  description: Verify that quantum core keeps its documented runtime behavior while handling memory conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_core.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_core.dart.md.
    stimulus:
    - Drive the registry/cache/state layer through hit, miss, mutation, and teardown cycles.
    - Repeat updates concurrently to expose stale snapshot or listener leaks.
    data:
      workload:
      - single write
      - parallel writes
      - rapid reads
      - eviction pressure
  expected_output:
    state: Snapshots remain internally consistent and deterministic.
    cleanup: Detached listeners and expired entries are reclaimed.
    performance: Hot-path operations stay bounded under repeated calls.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 100
    stress_ms: 250
  memory_budget:
    max_growth_mb: 1
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'This file defines the shared low-level primitives that the rest of the framework depends on: state flags, sleep policy, node errors, field type constants, field flags, projection helpers, path utilities, and a few compact math / parsing helpers. It is the first place to look when a higher layer needs to know what a field type or runtime bit flag means.'
    headings:
    - '`src/foundation/quantum_core.dart`'
    - What this file is
    - Core responsibilities
    - Key field types
    - Field flags
  tags:
  - runtime
  - memory
  - quantum core
- uuid: fdea4dd1-4377-59f6-9e1a-87387a2888a5
  file: lib/src/foundation/quantum_core.dart
  docs_file: docs/src/foundation/quantum_core.dart.md
  title: 'Quantum Core: the runtime surface still matches the documented API and headings'
  slug: docs_parity
  category: runtime
  severity: medium
  description: Verify that quantum core keeps its documented runtime behavior while handling docs parity conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_core.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_core.dart.md.
    stimulus:
    - Drive the registry/cache/state layer through hit, miss, mutation, and teardown cycles.
    - Repeat updates concurrently to expose stale snapshot or listener leaks.
    data:
      workload:
      - single write
      - parallel writes
      - rapid reads
      - eviction pressure
  expected_output:
    state: Snapshots remain internally consistent and deterministic.
    cleanup: Detached listeners and expired entries are reclaimed.
    performance: Hot-path operations stay bounded under repeated calls.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 100
    stress_ms: 250
  memory_budget:
    max_growth_mb: 1
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'This file defines the shared low-level primitives that the rest of the framework depends on: state flags, sleep policy, node errors, field type constants, field flags, projection helpers, path utilities, and a few compact math / parsing helpers. It is the first place to look when a higher layer needs to know what a field type or runtime bit flag means.'
    headings:
    - '`src/foundation/quantum_core.dart`'
    - What this file is
    - Core responsibilities
    - Key field types
    - Field flags
  tags:
  - runtime
  - docs_parity
  - quantum core
- uuid: 6847b9dd-9259-5aef-8a00-7c22f1db61d7
  file: lib/src/foundation/quantum_core.dart
  docs_file: docs/src/foundation/quantum_core.dart.md
  title: 'Quantum Core: registry and dispatch semantics remain correct under mixed workload'
  slug: registry
  category: runtime
  severity: medium
  description: Verify that quantum core keeps its documented runtime behavior while handling registry conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_core.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_core.dart.md.
    stimulus:
    - Drive the registry/cache/state layer through hit, miss, mutation, and teardown cycles.
    - Repeat updates concurrently to expose stale snapshot or listener leaks.
    data:
      workload:
      - single write
      - parallel writes
      - rapid reads
      - eviction pressure
  expected_output:
    state: Snapshots remain internally consistent and deterministic.
    cleanup: Detached listeners and expired entries are reclaimed.
    performance: Hot-path operations stay bounded under repeated calls.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 100
    stress_ms: 250
  memory_budget:
    max_growth_mb: 1
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'This file defines the shared low-level primitives that the rest of the framework depends on: state flags, sleep policy, node errors, field type constants, field flags, projection helpers, path utilities, and a few compact math / parsing helpers. It is the first place to look when a higher layer needs to know what a field type or runtime bit flag means.'
    headings:
    - '`src/foundation/quantum_core.dart`'
    - What this file is
    - Core responsibilities
    - Key field types
    - Field flags
  tags:
  - runtime
  - registry
  - quantum core
- uuid: dc952bef-ec07-574a-9668-d56d525cb3a5
  file: lib/src/foundation/quantum_core.dart
  docs_file: docs/src/foundation/quantum_core.dart.md
  title: 'Quantum Core: orchestration keeps routing, caching, and cleanup coherent'
  slug: orchestration
  category: runtime
  severity: medium
  description: Verify that quantum core keeps its documented runtime behavior while handling orchestration conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_core.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_core.dart.md.
    stimulus:
    - Drive the registry/cache/state layer through hit, miss, mutation, and teardown cycles.
    - Repeat updates concurrently to expose stale snapshot or listener leaks.
    data:
      workload:
      - single write
      - parallel writes
      - rapid reads
      - eviction pressure
  expected_output:
    state: Snapshots remain internally consistent and deterministic.
    cleanup: Detached listeners and expired entries are reclaimed.
    performance: Hot-path operations stay bounded under repeated calls.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 100
    stress_ms: 250
  memory_budget:
    max_growth_mb: 1
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'This file defines the shared low-level primitives that the rest of the framework depends on: state flags, sleep policy, node errors, field type constants, field flags, projection helpers, path utilities, and a few compact math / parsing helpers. It is the first place to look when a higher layer needs to know what a field type or runtime bit flag means.'
    headings:
    - '`src/foundation/quantum_core.dart`'
    - What this file is
    - Core responsibilities
    - Key field types
    - Field flags
  tags:
  - runtime
  - orchestration
  - quantum core
- uuid: 4d78bd11-9c83-5529-8a33-f3dded21f265
  file: lib/src/foundation/quantum_core.dart
  docs_file: docs/src/foundation/quantum_core.dart.md
  title: 'Quantum Core: integration boundaries stay stable across dependent subsystems'
  slug: integration
  category: runtime
  severity: medium
  description: Verify that quantum core keeps its documented runtime behavior while handling integration conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/foundation/quantum_core.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/foundation/quantum_core.dart.md.
    stimulus:
    - Drive the registry/cache/state layer through hit, miss, mutation, and teardown cycles.
    - Repeat updates concurrently to expose stale snapshot or listener leaks.
    data:
      workload:
      - single write
      - parallel writes
      - rapid reads
      - eviction pressure
  expected_output:
    state: Snapshots remain internally consistent and deterministic.
    cleanup: Detached listeners and expired entries are reclaimed.
    performance: Hot-path operations stay bounded under repeated calls.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 100
    stress_ms: 250
  memory_budget:
    max_growth_mb: 1
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'This file defines the shared low-level primitives that the rest of the framework depends on: state flags, sleep policy, node errors, field type constants, field flags, projection helpers, path utilities, and a few compact math / parsing helpers. It is the first place to look when a higher layer needs to know what a field type or runtime bit flag means.'
    headings:
    - '`src/foundation/quantum_core.dart`'
    - What this file is
    - Core responsibilities
    - Key field types
    - Field flags
  tags:
  - runtime
  - integration
  - quantum core
