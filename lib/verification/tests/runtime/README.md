# Runtime test shards

These YAML files are the executable test catalog used by the generated Dart suites.

Output layout when the runtime suite writes results:
- `tests/runtime/results/pass/` for passing case summaries
- `tests/runtime/results/fail/` for failing case summaries
- `tests/runtime/results/summary/` for per-file rollups
