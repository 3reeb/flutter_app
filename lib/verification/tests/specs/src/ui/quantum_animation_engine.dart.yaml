file: lib/src/ui/quantum_animation_engine.dart
docs_file: docs/src/ui/quantum_animation_engine.dart.md
line_count: 1295
category: visual
tests:
- uuid: 3755e03b-b2cb-543d-8185-12b2e6a02d31
  file: lib/src/ui/quantum_animation_engine.dart
  docs_file: docs/src/ui/quantum_animation_engine.dart.md
  title: 'Quantum Animation Engine: public contract remains stable under valid input'
  slug: contract
  category: visual
  severity: critical
  description: Verify that quantum animation engine keeps its documented runtime behavior
    while handling contract conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/ui/quantum_animation_engine.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/ui/quantum_animation_engine.dart.md.
    stimulus:
    - Exercise the runtime feature under nominal, stress, and teardown conditions.
    - Repeat the operation with large inputs or rapid state changes to expose regressions.
    data:
      size:
      - small
      - large
      - stress
      mutation:
      - none
      - single update
      - burst updates
  expected_output:
    correctness: Observed output matches the contract for the selected scenario.
    performance: Latency and memory stay inside the budget.
    cleanup: No dangling listener, buffer, or cache remains after teardown.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A subsystem engine. Files with this role typically own state, lifecycle,
      rendering, decoding, or orchestration for one feature area and expose a higher-level
      API to the rest of the framework. Author-intent note: QUANTUM ANIMATION ENGINE
      v2.0 — OMEGA TIMELINE + iOS SPRING CORE'
    headings:
    - '`src/ui/quantum_animation_engine.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - visual
  - contract
  - quantum animation engine
- uuid: c1a1fd1e-1ddc-5ec4-8a55-1f44da8078b1
  file: lib/src/ui/quantum_animation_engine.dart
  docs_file: docs/src/ui/quantum_animation_engine.dart.md
  title: 'Quantum Animation Engine: invalid or malformed input is rejected cleanly'
  slug: invalid_input
  category: visual
  severity: critical
  description: Verify that quantum animation engine keeps its documented runtime behavior
    while handling invalid input conditions without corruption, leakage, or silent
    failure.
  input:
    preconditions:
    - Load src/ui/quantum_animation_engine.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/ui/quantum_animation_engine.dart.md.
    stimulus:
    - Exercise the runtime feature under nominal, stress, and teardown conditions.
    - Repeat the operation with large inputs or rapid state changes to expose regressions.
    data:
      size:
      - small
      - large
      - stress
      mutation:
      - none
      - single update
      - burst updates
  expected_output:
    correctness: Observed output matches the contract for the selected scenario.
    performance: Latency and memory stay inside the budget.
    cleanup: No dangling listener, buffer, or cache remains after teardown.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A subsystem engine. Files with this role typically own state, lifecycle,
      rendering, decoding, or orchestration for one feature area and expose a higher-level
      API to the rest of the framework. Author-intent note: QUANTUM ANIMATION ENGINE
      v2.0 — OMEGA TIMELINE + iOS SPRING CORE'
    headings:
    - '`src/ui/quantum_animation_engine.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - visual
  - invalid_input
  - quantum animation engine
- uuid: 0e682d83-7333-53e5-afb6-1d3bec289182
  file: lib/src/ui/quantum_animation_engine.dart
  docs_file: docs/src/ui/quantum_animation_engine.dart.md
  title: 'Quantum Animation Engine: re-entrant calls do not corrupt internal state'
  slug: reentrant
  category: visual
  severity: high
  description: Verify that quantum animation engine keeps its documented runtime behavior
    while handling reentrant conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/ui/quantum_animation_engine.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/ui/quantum_animation_engine.dart.md.
    stimulus:
    - Exercise the runtime feature under nominal, stress, and teardown conditions.
    - Repeat the operation with large inputs or rapid state changes to expose regressions.
    data:
      size:
      - small
      - large
      - stress
      mutation:
      - none
      - single update
      - burst updates
  expected_output:
    correctness: Observed output matches the contract for the selected scenario.
    performance: Latency and memory stay inside the budget.
    cleanup: No dangling listener, buffer, or cache remains after teardown.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A subsystem engine. Files with this role typically own state, lifecycle,
      rendering, decoding, or orchestration for one feature area and expose a higher-level
      API to the rest of the framework. Author-intent note: QUANTUM ANIMATION ENGINE
      v2.0 — OMEGA TIMELINE + iOS SPRING CORE'
    headings:
    - '`src/ui/quantum_animation_engine.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - visual
  - reentrant
  - quantum animation engine
- uuid: ba3f5026-a891-5d8a-b4cd-49c58a467741
  file: lib/src/ui/quantum_animation_engine.dart
  docs_file: docs/src/ui/quantum_animation_engine.dart.md
  title: 'Quantum Animation Engine: dispose/close/teardown releases resources deterministically'
  slug: cleanup
  category: visual
  severity: high
  description: Verify that quantum animation engine keeps its documented runtime behavior
    while handling cleanup conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/ui/quantum_animation_engine.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/ui/quantum_animation_engine.dart.md.
    stimulus:
    - Exercise the runtime feature under nominal, stress, and teardown conditions.
    - Repeat the operation with large inputs or rapid state changes to expose regressions.
    data:
      size:
      - small
      - large
      - stress
      mutation:
      - none
      - single update
      - burst updates
  expected_output:
    correctness: Observed output matches the contract for the selected scenario.
    performance: Latency and memory stay inside the budget.
    cleanup: No dangling listener, buffer, or cache remains after teardown.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A subsystem engine. Files with this role typically own state, lifecycle,
      rendering, decoding, or orchestration for one feature area and expose a higher-level
      API to the rest of the framework. Author-intent note: QUANTUM ANIMATION ENGINE
      v2.0 — OMEGA TIMELINE + iOS SPRING CORE'
    headings:
    - '`src/ui/quantum_animation_engine.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - visual
  - cleanup
  - quantum animation engine
- uuid: d2a3eda4-2a46-5e76-9667-96987215b85f
  file: lib/src/ui/quantum_animation_engine.dart
  docs_file: docs/src/ui/quantum_animation_engine.dart.md
  title: 'Quantum Animation Engine: hot-path behavior stays within the runtime budget'
  slug: performance
  category: visual
  severity: high
  description: Verify that quantum animation engine keeps its documented runtime behavior
    while handling performance conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/ui/quantum_animation_engine.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/ui/quantum_animation_engine.dart.md.
    stimulus:
    - Exercise the runtime feature under nominal, stress, and teardown conditions.
    - Repeat the operation with large inputs or rapid state changes to expose regressions.
    data:
      size:
      - small
      - large
      - stress
      mutation:
      - none
      - single update
      - burst updates
  expected_output:
    correctness: Observed output matches the contract for the selected scenario.
    performance: Latency and memory stay inside the budget.
    cleanup: No dangling listener, buffer, or cache remains after teardown.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A subsystem engine. Files with this role typically own state, lifecycle,
      rendering, decoding, or orchestration for one feature area and expose a higher-level
      API to the rest of the framework. Author-intent note: QUANTUM ANIMATION ENGINE
      v2.0 — OMEGA TIMELINE + iOS SPRING CORE'
    headings:
    - '`src/ui/quantum_animation_engine.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - visual
  - performance
  - quantum animation engine
- uuid: f9c8b578-35b7-5ffc-8bbf-d7c849eed1ba
  file: lib/src/ui/quantum_animation_engine.dart
  docs_file: docs/src/ui/quantum_animation_engine.dart.md
  title: 'Quantum Animation Engine: memory usage stays bounded under repeated operations'
  slug: memory
  category: visual
  severity: medium
  description: Verify that quantum animation engine keeps its documented runtime behavior
    while handling memory conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/ui/quantum_animation_engine.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/ui/quantum_animation_engine.dart.md.
    stimulus:
    - Exercise the runtime feature under nominal, stress, and teardown conditions.
    - Repeat the operation with large inputs or rapid state changes to expose regressions.
    data:
      size:
      - small
      - large
      - stress
      mutation:
      - none
      - single update
      - burst updates
  expected_output:
    correctness: Observed output matches the contract for the selected scenario.
    performance: Latency and memory stay inside the budget.
    cleanup: No dangling listener, buffer, or cache remains after teardown.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A subsystem engine. Files with this role typically own state, lifecycle,
      rendering, decoding, or orchestration for one feature area and expose a higher-level
      API to the rest of the framework. Author-intent note: QUANTUM ANIMATION ENGINE
      v2.0 — OMEGA TIMELINE + iOS SPRING CORE'
    headings:
    - '`src/ui/quantum_animation_engine.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - visual
  - memory
  - quantum animation engine
- uuid: 36f974fb-15ce-5235-8bf5-8e120b07785f
  file: lib/src/ui/quantum_animation_engine.dart
  docs_file: docs/src/ui/quantum_animation_engine.dart.md
  title: 'Quantum Animation Engine: the runtime surface still matches the documented
    API and headings'
  slug: docs_parity
  category: visual
  severity: medium
  description: Verify that quantum animation engine keeps its documented runtime behavior
    while handling docs parity conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/ui/quantum_animation_engine.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/ui/quantum_animation_engine.dart.md.
    stimulus:
    - Exercise the runtime feature under nominal, stress, and teardown conditions.
    - Repeat the operation with large inputs or rapid state changes to expose regressions.
    data:
      size:
      - small
      - large
      - stress
      mutation:
      - none
      - single update
      - burst updates
  expected_output:
    correctness: Observed output matches the contract for the selected scenario.
    performance: Latency and memory stay inside the budget.
    cleanup: No dangling listener, buffer, or cache remains after teardown.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A subsystem engine. Files with this role typically own state, lifecycle,
      rendering, decoding, or orchestration for one feature area and expose a higher-level
      API to the rest of the framework. Author-intent note: QUANTUM ANIMATION ENGINE
      v2.0 — OMEGA TIMELINE + iOS SPRING CORE'
    headings:
    - '`src/ui/quantum_animation_engine.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - visual
  - docs_parity
  - quantum animation engine
- uuid: 1df910fc-ecf5-5be2-81d6-fcfccb00852b
  file: lib/src/ui/quantum_animation_engine.dart
  docs_file: docs/src/ui/quantum_animation_engine.dart.md
  title: 'Quantum Animation Engine: rendering remains correct for large, dense, and
    animated content'
  slug: render
  category: visual
  severity: medium
  description: Verify that quantum animation engine keeps its documented runtime behavior
    while handling render conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/ui/quantum_animation_engine.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/ui/quantum_animation_engine.dart.md.
    stimulus:
    - Exercise the runtime feature under nominal, stress, and teardown conditions.
    - Repeat the operation with large inputs or rapid state changes to expose regressions.
    data:
      size:
      - small
      - large
      - stress
      mutation:
      - none
      - single update
      - burst updates
  expected_output:
    correctness: Observed output matches the contract for the selected scenario.
    performance: Latency and memory stay inside the budget.
    cleanup: No dangling listener, buffer, or cache remains after teardown.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A subsystem engine. Files with this role typically own state, lifecycle,
      rendering, decoding, or orchestration for one feature area and expose a higher-level
      API to the rest of the framework. Author-intent note: QUANTUM ANIMATION ENGINE
      v2.0 — OMEGA TIMELINE + iOS SPRING CORE'
    headings:
    - '`src/ui/quantum_animation_engine.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - visual
  - render
  - quantum animation engine
- uuid: b5cf7f2d-81ef-5c07-b591-3164ab9ace35
  file: lib/src/ui/quantum_animation_engine.dart
  docs_file: docs/src/ui/quantum_animation_engine.dart.md
  title: 'Quantum Animation Engine: pointer hover and selection feedback stay synchronized
    with paint state'
  slug: hover
  category: visual
  severity: medium
  description: Verify that quantum animation engine keeps its documented runtime behavior
    while handling hover conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/ui/quantum_animation_engine.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/ui/quantum_animation_engine.dart.md.
    stimulus:
    - Exercise the runtime feature under nominal, stress, and teardown conditions.
    - Repeat the operation with large inputs or rapid state changes to expose regressions.
    data:
      size:
      - small
      - large
      - stress
      mutation:
      - none
      - single update
      - burst updates
  expected_output:
    correctness: Observed output matches the contract for the selected scenario.
    performance: Latency and memory stay inside the budget.
    cleanup: No dangling listener, buffer, or cache remains after teardown.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A subsystem engine. Files with this role typically own state, lifecycle,
      rendering, decoding, or orchestration for one feature area and expose a higher-level
      API to the rest of the framework. Author-intent note: QUANTUM ANIMATION ENGINE
      v2.0 — OMEGA TIMELINE + iOS SPRING CORE'
    headings:
    - '`src/ui/quantum_animation_engine.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - visual
  - hover
  - quantum animation engine
- uuid: 5da95d59-8576-5f20-9fc4-e5a1ddb6ccc8
  file: lib/src/ui/quantum_animation_engine.dart
  docs_file: docs/src/ui/quantum_animation_engine.dart.md
  title: 'Quantum Animation Engine: frame budget remains acceptable when the view
    is stressed'
  slug: frame
  category: visual
  severity: medium
  description: Verify that quantum animation engine keeps its documented runtime behavior
    while handling frame conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/ui/quantum_animation_engine.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/ui/quantum_animation_engine.dart.md.
    stimulus:
    - Exercise the runtime feature under nominal, stress, and teardown conditions.
    - Repeat the operation with large inputs or rapid state changes to expose regressions.
    data:
      size:
      - small
      - large
      - stress
      mutation:
      - none
      - single update
      - burst updates
  expected_output:
    correctness: Observed output matches the contract for the selected scenario.
    performance: Latency and memory stay inside the budget.
    cleanup: No dangling listener, buffer, or cache remains after teardown.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A subsystem engine. Files with this role typically own state, lifecycle,
      rendering, decoding, or orchestration for one feature area and expose a higher-level
      API to the rest of the framework. Author-intent note: QUANTUM ANIMATION ENGINE
      v2.0 — OMEGA TIMELINE + iOS SPRING CORE'
    headings:
    - '`src/ui/quantum_animation_engine.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - visual
  - frame
  - quantum animation engine
- uuid: ee88522a-939d-5f58-84d7-92727fc34b9e
  file: lib/src/ui/quantum_animation_engine.dart
  docs_file: docs/src/ui/quantum_animation_engine.dart.md
  title: 'Quantum Animation Engine: snapshot/export paths preserve visual fidelity
    and do not leak buffers'
  slug: export
  category: visual
  severity: medium
  description: Verify that quantum animation engine keeps its documented runtime behavior
    while handling export conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/ui/quantum_animation_engine.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/ui/quantum_animation_engine.dart.md.
    stimulus:
    - Exercise the runtime feature under nominal, stress, and teardown conditions.
    - Repeat the operation with large inputs or rapid state changes to expose regressions.
    data:
      size:
      - small
      - large
      - stress
      mutation:
      - none
      - single update
      - burst updates
  expected_output:
    correctness: Observed output matches the contract for the selected scenario.
    performance: Latency and memory stay inside the budget.
    cleanup: No dangling listener, buffer, or cache remains after teardown.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A subsystem engine. Files with this role typically own state, lifecycle,
      rendering, decoding, or orchestration for one feature area and expose a higher-level
      API to the rest of the framework. Author-intent note: QUANTUM ANIMATION ENGINE
      v2.0 — OMEGA TIMELINE + iOS SPRING CORE'
    headings:
    - '`src/ui/quantum_animation_engine.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - visual
  - export
  - quantum animation engine
- uuid: c43f5878-4815-5801-8b62-effd2548b6b8
  file: lib/src/ui/quantum_animation_engine.dart
  docs_file: docs/src/ui/quantum_animation_engine.dart.md
  title: 'Quantum Animation Engine: public contract remains stable under valid input'
  slug: contract
  category: visual
  severity: medium
  description: Verify that quantum animation engine keeps its documented runtime behavior
    while handling contract conditions without corruption, leakage, or silent failure.
  input:
    preconditions:
    - Load src/ui/quantum_animation_engine.dart with its documented dependencies available.
    - Use the runtime surface described in docs/src/ui/quantum_animation_engine.dart.md.
    stimulus:
    - Exercise the runtime feature under nominal, stress, and teardown conditions.
    - Repeat the operation with large inputs or rapid state changes to expose regressions.
    data:
      size:
      - small
      - large
      - stress
      mutation:
      - none
      - single update
      - burst updates
  expected_output:
    correctness: Observed output matches the contract for the selected scenario.
    performance: Latency and memory stay inside the budget.
    cleanup: No dangling listener, buffer, or cache remains after teardown.
  observability:
    logs: No unhandled exceptions, uncaught async errors, or hidden retries.
    metrics: Capture latency, allocations, rebuild count, and cleanup completion.
    traces: Trace the call path through the documented public API and teardown hooks.
  performance_budget:
    max_ms: 50
    stress_ms: 150
  memory_budget:
    max_growth_mb: 4
    leak_check: true
  cleanup:
  - Dispose controllers, streams, painters, or isolates.
  - Verify no retained listeners or stale caches remain.
  docs_parity:
    summary: 'A subsystem engine. Files with this role typically own state, lifecycle,
      rendering, decoding, or orchestration for one feature area and expose a higher-level
      API to the rest of the framework. Author-intent note: QUANTUM ANIMATION ENGINE
      v2.0 — OMEGA TIMELINE + iOS SPRING CORE'
    headings:
    - '`src/ui/quantum_animation_engine.dart`'
    - What this file is
    - Dependencies
    - Top-level declarations
    - Important members and helpers
  tags:
  - visual
  - contract
  - quantum animation engine
