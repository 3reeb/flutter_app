schema_version: 1
doc_kind: runtime-execution-file-test-manifest
file: lib/plugins/quantum_media_api
source_path: plugins/quantum_media_api
launch_mode: app_startup
execution_only: true
area: quantum_media_api
test_doc_status: draft
source_metadata:
  sha256: 7fc8e368629c4446615cf0e57207e0de4f57dbdfb950d22080279c3f62537a04
  line_count: 939
  imports:
  - dart:async
  - dart:convert
  - dart:io
  - dart:math
  - dart:typed_data
  - dart:collection
  - package:crypto/crypto.dart
  - package:flutter/foundation.dart
  - quantum_api_engine.dart
  symbols:
  - TransferProgress
  - ByteRange
  - RangeTracker
  - MediaCacheManager
  - BandwidthEstimator
  - MediaPrefetcher
  - ResumableUploader
  - LocalMediaProxyServer
  - StreamSegment
  - AdaptiveManifest
  - AdaptiveMediaStreamer
  - VoipPacket
  - LiveMediaPipeline
  - QuantumMediaEngine
  - MediaType
  - Quality
  - HttpMethod
  - contains
  - overlaps
  - addRange
runtime_focus:
- invalid uri or endpoint
- timeout / disconnected transport
- empty response body
- protocol mismatch
test_cases:
- id: case-1
  title: null input
  stimulus: null / missing input
  runtime_failure_target: null_pointer_access
  expected:
  - surface a runtime error or rejection
  - exercise the live code path during app launch or harness execution
  - avoid static verification of code existence
  - clean up resources and leave no stale runtime state
- id: case-2
  title: malformed input
  stimulus: invalid / malformed payload
  runtime_failure_target: parse_or_type_failure
  expected:
  - surface a runtime error or rejection
  - exercise the live code path during app launch or harness execution
  - avoid static verification of code existence
  - clean up resources and leave no stale runtime state
- id: case-3
  title: boundary input
  stimulus: empty / oversized / zero-value edge case
  runtime_failure_target: range_or_state_failure
  expected:
  - surface a runtime error or rejection
  - exercise the live code path during app launch or harness execution
  - avoid static verification of code existence
  - clean up resources and leave no stale runtime state
- id: case-4
  title: resource pressure
  stimulus: large batch / repeated execution / allocation stress
  runtime_failure_target: out_of_memory_or_allocation_failure
  expected:
  - surface a runtime error or rejection
  - exercise the live code path during app launch or harness execution
  - avoid static verification of code existence
  - clean up resources and leave no stale runtime state
assertions:
  must_execute_live_code: true
  must_capture_runtime_failure: true
  must_not_use_static_verification: true
  must_verify_cleanup: true
