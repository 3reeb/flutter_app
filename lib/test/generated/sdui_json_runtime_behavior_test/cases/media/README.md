# Media cases

These cases focus on media node normalization and prop preservation.

The SDUI runtime behavior runner discovers this folder recursively, so these
fixtures stay separated from the rest of the catalog while still being executed
by the main `sdui_json_runtime_behavior_test.dart` runner.
